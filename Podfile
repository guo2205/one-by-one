# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'one by one' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for one by one

  target 'one by oneTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'one by oneUITests' do
    inherit! :search_paths
    # Pods for testing
  end
pod 'Moya'
pod 'Pgyer'
pod 'PgyUpdate'
pod 'SwiftDate'
end
